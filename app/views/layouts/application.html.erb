<!DOCTYPE html>
<html>
  <head>
    <title>DemoTurboFrames</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <main class="flex flex-col relative container h-screen mx-auto  max-w-sm bg-white">
      <div class="flex-grow px-5">
        <%= turbo_frame_tag :main_content do %>
          <%= yield %>
        <% end %>
      </div>

      <div class="flex-shrink-0 absolute bottom-0 tabs flex items-center w-full h-16 border border-t  text-sm font-medium text-gray-700">
        <div class="h-full w-1/2">
          <%= link_to cities_path, class: 'flex w-full h-full items-center justify-center', data: { turbo_frame: :main_content } do %>
            <span>Cities</span>
          <% end %>
        </div>
        <div class="border-l h-full w-1/2">
          <%= link_to states_path, class: 'flex h-full w-full items-center justify-center', data: { turbo_frame: :main_content } do %>
            <span>States</span>
          <% end %>
        </div>
      </div>
    </main>
  </body>
</html>
